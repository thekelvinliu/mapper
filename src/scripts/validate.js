'use strict';

// const validate = () => /^[a-zA-Z0-9]+$/.test(document.forms['register']['user'].value);

function validate() {
  return /^[a-zA-Z0-9]+$/.test(document.forms['register']['user'].value);
}
